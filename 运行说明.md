# 矢量图形编辑器 - 运行说明

## 快速启动

### 方法一：直接运行
```bash
python start.py
```

### 方法二：运行源码
```bash
cd src
python main.py
```

## 功能验证

### 基础功能检查
1. 程序能正常启动
2. 工具栏显示正常
3. 图形视图显示正常
4. 属性面板显示正常

### 测试步骤
1. 运行程序，查看界面是否正常
2. 点击工具栏的不同工具按钮
3. 在画布上尝试绘制图形（需要实现绘图功能）
4. 选择图形，查看属性面板是否更新

## 项目结构

```
vector_editor/
├── src/              # 源代码
│   ├── core/         # 核心模块
│   ├── ui/           # 用户界面
│   ├── widgets/      # 自定义控件
│   ├── tools/        # 工具类
│   ├── libraries/    # 图元库
│   └── utils/        # 工具函数
├── data/             # 数据文件
├── resources/        # 资源文件
├── tests/            # 测试文件
└── start.py          # 启动脚本
```

## 开发注意事项

1. 每次修改代码后，建议使用 `python start.py` 测试程序是否仍能正常启动
2. 新增功能时，注意保持与现有功能的兼容性
3. 图形系统使用 PyQt5 的 QGraphicsView 框架
4. 所有图形数据使用 JSON 格式序列化